require 'spec_helper'

feature "hidden links" do
	let(:user) { create(:user) }
	let(:faculty_user) { create(:faculty_user) }
	let(:admin_user) { create(:admin_user) }

	# add hidden links spec on NAV
end
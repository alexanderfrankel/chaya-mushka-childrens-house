<table class="table">

	<thead>
		<tr>
			<th>Date</th>
			<th>Title</th>
			<th>Content</th>
			<th>User</th>
		</tr>
	</thead>

	<tbody>
		<% @posts.each do |post| %>
			<tr>
				<td><%= post.created_at %></td>
				<td><%= link_to post.title, admin_post_path(post) %></td>
				<td><%= post.content %></td>
				<td><%= post.user %></td>
			</tr>
		<% end %>
	</tbody>

</table>